// Import dependencies
import React from 'react';
import { Text } from 'react-native';

// Make component
const Header = () => {
  return <Text>Albums</Text>;
};

// Export component
export default Header;